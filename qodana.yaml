version: "1.0"
profile:
  name: qodana.recommended

dotnet:
  solution: SoulSplitter.sln

include:
    - name: NetCoverageInspection

exclude:
  - name: exclude
    paths:
      - src/cli
      - tests